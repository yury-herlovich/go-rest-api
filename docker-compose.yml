version: '3.8'
services:

  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: dev
    command: make watch
    volumes:
      - ./:/usr/src
    ports:
      - 8080:8080
